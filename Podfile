# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'databinding' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'RxSwift', '~> 3.0'
  pod 'RxCocoa', '~> 3.0'

  # Pods for databinding

  target 'databindingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'databindingUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
